#coding=utf-8
'''
Created on 2015年12月25日

@author: admin
'''
from fabric.api import *
env.host_string="root@119.39.48.91:10002"
env.password="JiaDe~!234"
file="/opt/back/511/huilaapi/src/Application/Biz/Conf/config.php"
com="""if [ -f "%s" ]; then
           rm -f %s
        fi""" % (file,file)
run(com)


"""from Tkinter import *
import tkMessageBox

msg=tkMessageBox.showinfo("提示：","没有找到可用配置文件，请手动设置相关参数！")
root=Tk()
sb = Scrollbar(root)
sb.pack(side=RIGHT, fill=Y)"""

"""from tkinter import *

root = Tk()

sb = Scrollbar(root)
sb.pack(side=RIGHT, fill=Y)

lb = Listbox(root, yscrollcommand=sb.set)

for i in range(1000):
&nbsp; &nbsp; lb.insert(END, str(i))

lb.pack(side=LEFT, fill=BOTH)

sb.config(command=lb.yview)

mainloop()"""